using showcase.gatway.Extensions;
using System.Net;

namespace showcase.gatway.Delegates.Showcase.Domain
{
    public class SavePersonHandler : DelegateBase
    {
        public SavePersonHandler(DelegateOption option) : base(option)
        {
        }
    }
}
